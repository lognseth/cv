# Remaining publication details

The résumé content has been populated from `Profile.pdf`.

Before launch, only these details remain:

- Add the final public GitHub repository URL to the “This website” card.
- Confirm that `linkedin@lognseth.de` is the preferred public contact address.
- Optionally add a clean downloadable PDF résumé. The supplied LinkedIn export contains extraneous injected text in its summary and should not be published as-is.
- Optionally add a headshot; the current design deliberately works without one.

